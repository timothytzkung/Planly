{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend/server && node server.js",
    "front-install": "cd frontend && npm install",
    "back-install": "cd backend && npm install",
    "setup": "run-s front-install back-install",
    "start": "run-p frontend backend"
  }
}
